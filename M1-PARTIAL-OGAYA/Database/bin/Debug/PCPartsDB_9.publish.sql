/*
Deployment script for PCPartsDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "PCPartsDB"
:setvar DefaultFilePrefix "PCPartsDB"
:setvar DefaultDataPath "C:\Users\dawor\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\dawor\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering Procedure [dbo].[spParts_Build]...';


GO
ALTER PROCEDURE [dbo].[spParts_Build]
  @PCPart NVARCHAR(MAX),
  @Id INT
AS
BEGIN
  SET NOCOUNT ON;

  DECLARE @TotalPrice DECIMAL(10, 2);
  SET @TotalPrice = 0;

  DECLARE @TableName NVARCHAR(MAX);
  SET @TableName = QUOTENAME(@PCPart);

  DECLARE @SQL NVARCHAR(MAX);
  SET @SQL = N'SELECT @TotalPrice = @TotalPrice + [UnitPrice] FROM ' + @TableName + N' WHERE [Id] = @Id';

  EXEC sp_executesql @SQL, N'@TotalPrice DECIMAL(10, 2) OUTPUT, @Id INT', @TotalPrice OUTPUT, @Id;

  SELECT @PCPart AS [PCPart], [Name], @TotalPrice AS [UnitPrice] FROM @TableName WHERE [Id] = @Id;
END
GO
PRINT N'Update complete.';


GO
